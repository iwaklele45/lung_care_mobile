# Optional. If omitted, the Firebase project id/local fallback is used.
# Set this before production deployment if Firestore reset documents may ever be readable outside Admin SDK.
OTP_PEPPER=replace-with-long-random-secret
